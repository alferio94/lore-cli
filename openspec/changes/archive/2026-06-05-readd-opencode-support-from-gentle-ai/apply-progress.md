# Apply Progress: readd-opencode-support-from-gentle-ai

## Status
- Mode: Standard (Strict TDD not active for this change)
- Current slice: completed (cleanup slice: copy/test-name polish to resolve the verify-report PASS WITH WARNINGS warnings, no behavior changes)
- Completed tasks: 10/14 from the original change + 4 cleanup-slice micro-tasks (A install usage line, B flag descriptions, C test rename + token, D focused assertions) + the supporting test updates from prior slices
  - Prior 3.3 + 3.4 slice completions (already on disk before this cleanup slice): 1.1, 1.2, 1.3, 2.1, 2.2, 3.1, 3.2, 3.3, 3.4 — all `[x]`.
  - This cleanup slice: A (install usage line), B (--target/--component flag descriptions), C (rename `RejectsOpenCodeTarget` → `AcceptsOpenCodeTarget` + token name + docstring), D (3 new positive assertions + 1 defensive negative assertion in `TestInstallUsageIncludesTargetAndComponentFlags`).
  - 4.1 + 4.2 remain (focused-verify tasks delegated to `sdd-verify`).

## Completed Tasks Cumulative
- [x] 1.1 Reintroduce `TargetOpenCode` and register `defaultOpenCodeAdapter()` in the default install registry.
- [x] 1.2 Add `internal/install/adapter_opencode.go` + `opencode_install.go` skeleton rendering AGENTS.md, skills/<phase>/SKILL.md, opencode.json (Pi/Antigravity-shaped remote MCP with `mcp.lore.managed_by: lore-cli` ownership marker), and manifest.
- [x] 1.3 Add managed OpenCode plugin assets under `internal/install/assets/opencode/plugins/` (background-agents.ts, model-variants.ts, opencode-subagent-statusline.ts) plus `internal/install/assets/opencode/tui.json` referencing the community statusline; explicit `sdd-engram` and `logo` exclusion list.
- [x] 2.1 RED: add negative tests in `internal/install/adapter_opencode_plugins_test.go`, `internal/install/opencode_install_test.go`, and `internal/install/static_guards_test.go` for no Gentle leakage, no excluded plugin names, redacted token output, and additive opencode.json merge/backup/idempotency.
- [x] 2.2 GREEN: make the OpenCode render/merge path pass those tests, including the bounded additive JSON merge for `opencode.json` and `tui.json`, backup-before-overwrite for existing user content, idempotent reruns, and the explicit `sdd-engram` / `logo` rejection gate.
- [x] 3.1 Restore OpenCode target copy in `internal/cli/{app.go,actions.go,install_flags_test.go}` and `internal/tui/{root.go,model_test.go}` so summaries describe config-only support, MCP, and plugin scope without Gentle wording.
- [x] 3.2 Update `README.md` and OpenCode install help to describe the re-added target, managed plugin assets, and explicit exclusions.
- [x] 3.3 Repair `mcp.lore` ownership/conflict semantics: add a `managed_by: lore-cli` marker to the rendered `mcp.lore` block, implement a typed `*OpenCodeMCPConfigOwnershipError`, scope the ownership check to `opencode.json` (so `tui.json` is unaffected), surface the conflict as a `conflicted` plan action with backup-before-abort semantics, and update tests/docs/copy to document the fail-closed contract.
- [x] 3.4 Clarify gentle plugin-registration semantics: tests/docs/copy now assert the local plugin .ts files (background-agents.ts, model-variants.ts) are copied to `~/.config/opencode/plugins/` and are NOT registered in `tui.json`; only the community `opencode-subagent-statusline` is referenced from `tui.json`. The defensive negative assertions in `internal/cli/install_flags_test.go` lock this contract in.
- [x] (cleanup A) Install usage short target list now includes `opencode` consistently with the accepted runtime behavior and the longer help copy: `Usage: lore install [--dry-run] [--yes] [--target pi|opencode|codex|antigravity] [--component <id>]`.
- [x] (cleanup B) `--target` flag description now mentions OpenCode alongside Codex and Antigravity as supported managed targets; `--component` flag description now includes OpenCode in the supported target set AND calls out that OpenCode also supports the `opencode-plugins` component.
- [x] (cleanup C) Renamed `TestInstallCommandRejectsOpenCodeTarget` → `TestInstallCommandAcceptsOpenCodeTarget` and refreshed the docstring; renamed the focused test token from `secret-token=opencode-rejected` → `secret-token=opencode-supported`. The test name no longer implies OpenCode is rejected.
- [x] (cleanup D) Extended `TestInstallUsageIncludesTargetAndComponentFlags` with 3 new positive assertions (lock the new install usage line, the new `--target` flag description, and the new `--component` flag description) plus 1 defensive negative assertion that locks out regression to `[--target pi|codex|antigravity]`.
- [x] (cumulative test updates from prior slices) Adapter opencode render test, plugin asset tests, install command tests, TUI model tests, and the service test for `TargetOpenCode.Description` assert the new ownership contract, the gentle plugin-registration semantics, and the fail-closed wording.

## Files Changed Cumulative
| File | Action | Task(s) | Notes |
|------|--------|---------|-------|
| `internal/install/service.go` | Modified | 1.1, 3.3 | `TargetOpenCode` constant, `DefaultTargets`/`SupportedTargets`, `supportedTarget` description (now mentions ownership contract + gentle plugin-registration). |
| `internal/install/adapter.go` | Modified | 1.1, 1.2 | Registered `defaultOpenCodeAdapter()`; `RenderRequest.Validate` OpenCode MCP auth gate. |
| `internal/install/components.go` | Modified | 1.1, 1.3 | Added `TargetOpenCode` to `ComponentCorePack.DefaultForTarget`; added `ComponentOpenCodePlugins` (default for OpenCode, optional). |
| `internal/install/harness.go` | Modified | 1.2 | `InstallRequest.Validate` OpenCode MCP auth gate. |
| `internal/install/adapter_opencode.go` | Modified | 1.2, 1.3, 3.1, 3.3 | OpenCode adapter + render helpers + `CapabilityOpenCodePlugins` + `renderOpenCodePluginAssets` + `matchesExcludedOpenCodePlugin` + plugins/tui.json render path; `renderOpenCodeAgentsMD` managed-surface copy now lists the bounded plugin bundle, exclusions, the plaintext-token warning, AND the `mcp.lore` ownership contract; `renderOpenCodeMCPConfig` now writes the `managed_by: lore-cli` marker on the `mcp.lore` block; `CapabilityOpenCodePlugins` description now spells out the gentle plugin-registration semantics. |
| `internal/install/opencode_install.go` | Modified | 1.2, 2.2, 3.3 | OpenCode plan/execute pipeline + additive JSON merge for `opencode.json` and `tui.json`; `planOpenCodeRenderedFileAction` now surfaces ownership conflicts as `conflicted` plan actions with backup-before-abort semantics; new `writeOpenCodeConflictBackup` helper. |
| `internal/install/opencode_assets.go` | Created | 1.3 | `opencodeInstallAssets` embed.FS; `managedOpenCodePluginAssetNames`; `excludedOpenCodePluginNames`; `readOpenCodePluginAsset` + `readOpenCodeTUISettingsAsset` + `opencodeEmbeddedAssetFS`. |
| `internal/install/json_merge.go` | Modified | 2.2, 3.3 | `mergeOpenCodeConfigJSON` (idempotent additive merge + scoped ownership check for `opencode.json`); new `*OpenCodeMCPConfigOwnershipError`, `IsOpenCodeMCPConfigOwnershipConflict` and `AsOpenCodeMCPConfigOwnershipConflict` helpers, `opencodeMCPLoreOwnership` helper, `inspectOpenCodeMCPConfigOwnership` inspector. |
| `internal/install/antigravity_install.go` | Modified | 3.3 | `appendInstallSummaryAction` now handles the `conflicted` action (records under `summary.Conflicted` and `summary.BackedUp`). |
| `internal/install/assets/opencode/plugins/background-agents.ts` | Created | 1.3 | Lore-managed background-agents plugin stub. |
| `internal/install/assets/opencode/plugins/model-variants.ts` | Created | 1.3 | Lore-managed model-variants plugin stub. |
| `internal/install/assets/opencode/plugins/opencode-subagent-statusline.ts` | Created | 1.3 | Community statusline reference stub. |
| `internal/install/assets/opencode/tui.json` | Created | 1.3 | OpenCode TUI settings JSON referencing the community statusline; declares the explicit `sdd-engram` / `logo` exclusion. |
| `internal/install/adapter_opencode_test.go` | Modified | 1.2, 1.3, 3.1, 3.3 | 7 positive tests + assertions for the bounded AGENTS.md managed-surface copy + assertion for `mcp.lore.managed_by == "lore-cli"` on the rendered payload. |
| `internal/install/adapter_opencode_plugins_test.go` | Created | 1.3, 2.1, 2.2 | 7 plugin regression tests. |
| `internal/install/opencode_install_test.go` | Created/Modified | 2.1, 2.2, 3.3 | 9 install-path regression tests (4 new fail-closed tests + the re-run-after-resolution test). |
| `internal/install/static_guards_test.go` | Modified | 2.1, 3.1 (no change) | Added 2 OpenCode static guards. |
| `internal/install/service_test.go` | Modified | 1.1, 1.2, 3.3 | Renamed to `…IncludesOpenCode`; `…AcceptsOpenCodeTarget`; new assertions for the `TargetOpenCode.Description` ownership contract + gentle plugin-registration. |
| `internal/install/adapter_test.go` | Modified | 1.1, 1.2, 1.3 | `Resolve(opencode)` success path; `DefaultComponentSelection(opencode)` returns `core-pack + opencode-plugins`. |
| `internal/cli/actions.go` | Modified | 1.2, 3.1, 3.3 | `installOpenCodeActionWithOptions`, `formatOpenCodeInstallSummary` / `formatOpenCodeInstallPlanSummary` (now emit `plugins=bundled:...`, `plugins_location=...`, `mcp_lore_ownership=fail-closed-on-foreign`, `excluded_plugins=sdd-engram,logo`, and a `conflicted=N` count in the apply summary), `opencode-config` check expanded with plaintext-token warning copy. |
| `internal/cli/app.go` | Modified | 3.1, 3.3, **cleanup A, cleanup B** | `runInstall` `--help` text documents the bounded `opencode-plugins` bundle, exclusions, the plaintext-token warning, the Ownership contract, AND (cleanup A) the install usage short target list now includes `opencode` (`Usage: lore install [--dry-run] [--yes] [--target pi|opencode|codex|antigravity] [--component <id>]`), AND (cleanup B) the `--target` flag description and the `--component` flag description now include OpenCode in the supported target set. |
| `internal/cli/install_flags_test.go` | Modified | 1.2, 3.1, 3.3, **cleanup C, cleanup D** | OpenCode dry-run test + `--help` test extended. **(cleanup C)** Renamed `TestInstallCommandRejectsOpenCodeTarget` → `TestInstallCommandAcceptsOpenCodeTarget`; refreshed the docstring; renamed the focused test token from `secret-token=opencode-rejected` → `secret-token=opencode-supported`. **(cleanup D)** Added 3 new positive assertions + 1 defensive negative assertion in `TestInstallUsageIncludesTargetAndComponentFlags` to lock the install usage short target list and the new flag descriptions. |
| `internal/tui/root.go` | Modified | 1.2, 3.1, 3.3 | Install menu description + target-selection footer mention bounded `opencode-plugins` bundle, the explicit `sdd-engram` / `logo` exclusions, the ownership contract, the `managed_by: lore-cli` marker, the fail-closed wording, AND the explicit "the local plugin .ts files are NOT registered in tui.json" sentence. |
| `internal/tui/model_test.go` | Modified | 1.2, 3.1, 3.3 | TUI target ordering for the re-add + new bounded plugin/exclusion/ownership assertions on the install menu description and target-selection body. |
| `README.md` | Modified | 1.2, 3.2, 3.3 | Root TUI description + OpenCode-managed-surface paragraph document the bounded `opencode-plugins` default component, the bundled plugin set, the explicit `sdd-engram` / `logo` exclusions, the plaintext-token warning that never embeds the saved token, the gentle plugin-registration semantics (local .ts files in plugins/, only the statusline in tui.json), AND the fail-closed `mcp.lore` ownership check. |
| `openspec/changes/readd-opencode-support-from-gentle-ai/apply-started.md` | Created | 1.1-1.3, 2.1, 2.2, 3.1, 3.2, 3.3, **cleanup slice** | Slice definition + preconditions (this file's primary scope was the 3.3 slice; the cleanup slice reuses the same checkpoint pattern with copy/test-name polish scope). |
| `openspec/changes/readd-opencode-support-from-gentle-ai/apply-partial.md` | Created | 1.1-1.3, 2.1, 2.2, 3.1, 3.2, 3.3, **cleanup slice** | Latest-slice partial (this slice = cleanup slice). |
| `openspec/changes/readd-opencode-support-from-gentle-ai/apply-progress.md` | Created | cumulative | This file. |
| `openspec/changes/readd-opencode-support-from-gentle-ai/apply-report.md` | Created | 1.1-1.3, 2.1, 2.2, 3.1, 3.2, 3.3, **cleanup slice** | Slice report (this slice = cleanup slice). |
| `openspec/changes/readd-opencode-support-from-gentle-ai/tasks.md` | Modified | 1.1, 1.2, 1.3, 2.1, 2.2, 3.1, 3.2, 3.3 | Marked completed tasks with `[x]`. (Cleanup slice is out-of-band; tasks.md is the original change checklist and remains at 12/14 — the cleanup slice resolves verify-report warnings, not the original change's task list.) |

## Validation Cumulative
| Command | Scope | Result | Notes |
|---------|-------|--------|-------|
| `go build ./internal/cli ./internal/install ./internal/tui` | entire CLI/install/TUI slice | passed | clean (this cleanup slice) |
| `go vet ./internal/cli ./internal/install ./internal/tui` | entire CLI/install/TUI slice | passed | clean (this cleanup slice) |
| `go test ./internal/cli -run 'TestInstallCommand\|TestInstallUsage' -count=1 -v` | focused CLI install tests | passed | all focused tests green, including the renamed `TestInstallCommandAcceptsOpenCodeTarget` and the extended `TestInstallUsageIncludesTargetAndComponentFlags` (this cleanup slice) |
| `go test ./internal/cli ./internal/install ./internal/tui -count=1` | full CLI/install/TUI slice | passed | all three packages green: `internal/cli` ok (1.835s), `internal/install` ok (0.931s), `internal/tui` ok (2.300s) — this is the orchestrator-requested validation command (this cleanup slice) |
| `go test ./internal/install -run 'TestOpenCodeConfigJSONMerge\|TestOpenCodePlanOpenCodeInstall' -count=1 -v` (prior 3.3 slice) | 11 focused tests | passed | all 11 fail-closed and additive-merge tests green |
| `go test ./internal/install -count=1` (prior 3.3 slice) | install package | passed | all install tests |
| `go test ./internal/cli -run 'TestInstallCommand' -count=1` (prior 3.3 slice) | CLI install command | passed | updated OpenCode dry-run + install --help |
| `go test ./internal/cli -count=1` (prior 3.3 slice) | full CLI package | passed | all CLI tests |
| `go test ./internal/tui -count=1` (prior 3.3 slice) | TUI package | passed | TUI install menu description and target-selection body |
| `go test ./internal/... -count=1` (prior 3.3 slice) | all internal packages | passed | install, cli, tui, agentpack, agentconfig, auth, config, httpclient, output, update, version |
| `go test ./... -count=1` (prior 3.3 slice) | full module | passed | entire module green |

## Deviations and Risks
- **Out-of-band cleanup slice**: this slice was added out-of-band after the verify report came back as `PASS WITH WARNINGS`. The two warnings the slice resolves are pure copy/test-name polish (install usage short target list missing `opencode`; legacy `TestInstallCommandRejectsOpenCodeTarget` test name implying rejection). The original change's `tasks.md` checklist remains at 12/14 (4.1 + 4.2 are focused-verify tasks delegated to `sdd-verify`).
- **Behavior preserved**: no runtime code path was modified by this slice. The bounded `mcp.lore` ownership check, the additive merge, the backup-before-overwrite, the idempotent reruns, the `opencode-plugins` default component, the `sdd-engram` / `logo` exclusion list, the `managed_by: lore-cli` marker, the fail-closed `*OpenCodeMCPConfigOwnershipError` typed conflict, and the plugin asset render path are all unchanged from the prior 3.3 + 3.4 slices.
- **Install usage line ordering**: the cleanup slice puts `opencode` second in the usage line (`pi|opencode|codex|antigravity`). This matches the `SupportedTargets` ordering in `internal/install/service.go` (`Pi, OpenCode, Codex, Antigravity`) and the README's "supported install target matrix is Pi, OpenCode, Codex, and Antigravity" wording. The longer help copy already follows this ordering (Pi paragraph, Antigravity paragraph, Codex paragraph, OpenCode paragraph). The slice does NOT change the longer help copy ordering — that was already consistent and is locked by `TestInstallUsageIncludesTargetAndComponentFlags` + TUI tests.
- **`TestInstallCommandAcceptsOpenCodeTarget` rename**: the test name now correctly reflects what the test asserts (OpenCode is accepted, with a dry-run). The prior name `TestInstallCommandRejectsOpenCodeTarget` was misleading after the 3.3 slice updated the test body to assert the accepted path. The test was the only stale-name artifact the verify report flagged.
- **Defensive negative assertion**: the cleanup slice adds a defensive negative assertion in `TestInstallUsageIncludesTargetAndComponentFlags` that the install usage short target list must NOT regress to `[--target pi|codex|antigravity]`. This locks the regression out of the test surface even if a future change rewrites the help copy without reading the verify-report warning list.
- **Lore memory unavailable**: this session has no `lore_*` MCP tools and `lore status` reports auth failure. Per the verify-report protocol and the launch-prompt fallback policy, all apply artifacts for this slice are persisted to the OpenSpec fallback store at `openspec/changes/readd-opencode-support-from-gentle-ai/`. If Lore memory becomes available, the next slice (or the next `sdd-archive` run) should sync these artifacts back into Lore under `sdd/readd-opencode-support-from-gentle-ai/apply-{started,partial,progress,report}` topic keys.

## Next Slice Recommendation
- Tasks: 4.1 + 4.2 (focused-verify tasks delegated to `sdd-verify`).
- Why these next: the cleanup slice closes both warnings from the verify report (install usage short target list now includes `opencode`; legacy `RejectsOpenCodeTarget` test renamed to `AcceptsOpenCodeTarget`). The bounded surface is now complete AND the user-facing copy is now consistent end-to-end. The 4.x slice should re-run the end-to-end install dry-run smoke against a real temp home (Pi, OpenCode, Codex, Antigravity) and confirm the verify report can be upgraded from `PASS WITH WARNINGS` to `PASS`. The bounded surface, the help copy, the test names, and the focused assertions are all in their final state — no further apply slices are needed.
- Why this ordering keeps slices small: each slice has had a single primary risk dimension (foundation → assets → regression gates → copy → ownership/repair → copy-polish cleanup). 4.x has zero primary risk: it is the verify slice and will be delegated to `sdd-verify` rather than this `sdd-apply` worker.
